# Agent vs Skills 拆分指南

本文档说明项目中 **agent** 和 **skills** 的区别及使用方式。

---

## 📖 核心概念

### 🎯 Agent（项目特定）
**"这个项目怎么做"**

存放位置：`.claude/agent/`

内容类型：
- 业务规则
- 项目约定
- 架构决策
- 测试配置
- Feature 清单

特点：
- ✅ 只对当前项目有效
- ✅ 包含业务逻辑
- ✅ 项目特定的约定

### 🔧 Skills（通用知识）
**"这个技术怎么用"**

存放位置：`.claude/skills/`

内容类型：
- 框架使用方法
- 技术最佳实践
- 标准编码模式
- 可复用方案

特点：
- ✅ 跨项目复用
- ✅ 技术导向
- ✅ 不包含业务逻辑

---

## 📂 目录结构

```
.claude/
├── skills/              # 通用技能（可复制到其他项目）
│   ├── beego-api/       # Beego v2 API 开发
│   ├── mysql/           # MySQL/ORM 操作
│   └── redis/           # Redis 缓存
│
├── agent/               # 项目特定内容
│   ├── instructions.md  # 项目约定（从 CLAUDE.md 自动加载）
│   └── context/
│       ├── business-modules.md  # 业务模块说明
│       └── code-standards.md    # 项目编码规范
│
└── templates/           # 新项目模板（可选）
    └── agent-template/  # 标准 agent 目录结构
```

---

## 🔍 内容分类示例

| 内容 | 类型 | 存放位置 |
|------|------|---------|
| Beego v2 vs v1 对比 | 🔧 Skill | `.claude/skills/beego-api/01-framework-basics.md` |
| Controller 继承模式 | 🔧 Skill | `.claude/skills/beego-api/02-controller-pattern.md` |
| DTO 定义规范 | 🔧 Skill | `.claude/skills/beego-api/05-dto-usage.md` |
| Swagger 注解 | 🔧 Skill | `.claude/skills/beego-api/07-swagger-annotation.md` |
| MySQL ORM 增删改查 | 🔧 Skill | `.claude/skills/mysql/01-orm-basic.md` |
| Redis 基础操作 | 🔧 Skill | `.claude/skills/redis/01-basic-operations.md` |
| **算力平台 25 个 Feature** | 🎯 Agent | `.claude/agent/context/business-modules.md` |
| **app_* 表设计** | 🎯 Agent | `.claude/agent/context/business-modules.md` |
| **测试 Token 和环境** | 🎯 Agent | `.claude/agent/context/code-standards.md` |
| **std-lib-slim 库使用** | 🎯 Agent | `.claude/agent/context/code-standards.md` |

---

## 🚀 新项目启动流程

### 1. 复制通用技能
```bash
# 创建新项目
mkdir new-project
cd new-project

# 复制技能库（通用）
cp -r ../xxx/.claude/skills .claude/skills
```

### 2. 创建项目特定 Agent
```bash
# 创建 agent 目录
mkdir -p .claude/agent/context

# 创建业务模块文档
touch .claude/agent/context/business-modules.md

# 创建编码规范文档
touch .claude/agent/context/code-standards.md
```

### 3. 填充 Agent 内容
根据新项目的业务需求，填写：
- 业务模块说明
- 数据库表设计
- 项目特定规范

### 4. CLAUDE.md 保持原样
`CLAUDE.md` 会自动加载为 `agent/instructions.md`，保持项目约定。

---

## 💡 何时应该拆分？

### ✅ 必须拆分的情况
1. **多项目共享技能** - 你有 3+ 个 Beego 项目
2. **团队协作** - 技能库全局共享，agent 项目特定
3. **大型项目** - 文档超过 500 行
4. **长期维护** - 方便后续更新和复用

### ⚠️ 可以不拆分的情况
1. **小型项目** - 少于 10 个文件
2. **个人学习** - 不需要复用
3. **快速原型** - 短期项目

---

## 📝 维护建议

### Skills 更新频率
- **低频更新** - 技术栈变化时（如 Beego v2 → v3）
- **版本控制** - 技能库可以单独 Git 仓库

### Agent 更新频率
- **高频更新** - 业务需求变化时
- **随项目走** - agent 跟随项目 Git 仓库

---

## 🎓 学习路径

### 阶段 1：单项目（当前）
```
.claude/
├── skills/      # 通用技能
├── agent/       # 项目特定
└── CLAUDE.md    # 项目约定（自动加载）
```

### 阶段 2：多项目
```
# 全局技能库（所有项目共享）
~/my-skills/
├── beego-api/
├── mysql/
└── redis/

# 项目 A
project-a/.claude/
├── skills -> ~/my-skills/  # 软链接
└── agent/                  # 项目 A 特定

# 项目 B
project-b/.claude/
├── skills -> ~/my-skills/  # 软链接
└── agent/                  # 项目 B 特定
```

---

## 🔗 相关文档

- **通用技能库**: `.claude/skills/`
- **项目业务模块**: `.claude/agent/context/business-modules.md`
- **项目编码规范**: `.claude/agent/context/code-standards.md`
- **项目约定（自动加载）**: `CLAUDE.md`

---

## 总结

| 维度 | Agent | Skills |
|------|-------|--------|
| **作用域** | 当前项目 | 所有项目 |
| **内容** | 业务规则、Feature 清单 | 技术使用、编码模式 |
| **更新频率** | 高（随需求变化） | 低（随技术变化） |
| **复用性** | 不可复用 | 可复用 |
| **示例** | "算力平台有25个Feature" | "Beego v2 Controller 继承" |

---

**当前项目采用的就是这种标准拆分方式！** ✅

---

# 模板仓库结构

本仓库是一个**通用项目模板**，包含三个独立可选模块。新项目可按需选取任意组合。

```
backend-front-template/
├── .claude/                    # 根级通用 AI 规范 & 技能库
│   ├── README.md              # 本文档（通用指南）
│   ├── SKILL.md               # Skills 索引
│   ├── skills/                # 通用技能（按需复制到新项目）
│   └── agent/                 # 项目特定业务上下文（模板）
│
├── backend/                   # 模块一：Go/Beego v2 后端
│   ├── CLAUDE.md              # ← 后端 AI 开发规范（自包含）
│   └── ...
│
├── frontend/
│   ├── flutter/               # 模块二：Flutter 移动端 App
│   │   ├── CLAUDE.md          # ← Flutter AI 开发规范（自包含）
│   │   └── init.sh            # ← 一键初始化脚本
│   │
│   └── vben-admin/            # 模块三：Vben Admin 管理后台
│       ├── CLAUDE.md          # ← Vben Admin AI 开发规范（自包含）
│       └── init.sh            # ← 一键初始化脚本
```

> **原则**：每个模块的 `CLAUDE.md` 是自包含的，复制该模块到新项目时 AI 规范跟着走。

---

## 各模块 AI 规范索引

| 模块 | 规范文件 | 初始化方式 |
|------|---------|-----------|
| 后端（Go/Beego v2） | `backend/CLAUDE.md` | 修改 go.mod + app.conf 即可 |
| Flutter App | `frontend/flutter/CLAUDE.md` | 运行 `init.sh` 一键创建 |
| Vben Admin 管理后台 | `frontend/vben-admin/CLAUDE.md` | 运行 `init.sh` 一键创建 |

---

## 新项目使用流程

```bash
# 1. 克隆模板
git clone <template-repo> my-new-project
cd my-new-project

# 2. 按需选取模块（不需要的直接删除）
# 例如：只要后端 + 管理后台，不要 Flutter
rm -rf frontend/flutter

# 3. 初始化后端
cd backend
cp conf/app.example.conf conf/app.conf
# 修改 go.mod 中的 module 名称
# 修改 conf/app.conf 中的数据库配置
# 修改 conf/const.go 中的 salt 和密钥

# 4. 初始化前端（如需要）
cd ../frontend/flutter && bash init.sh my_app com.yourcompany
# 或
cd ../frontend/vben-admin && bash init.sh

# 5. 创建项目特定的 AI 上下文
mkdir -p .claude/agent/context
# 填写 business-modules.md 和 code-standards.md
```

---

## 通用开发约定

### Git 分支规范

```
main        → 生产分支（受保护）
develop     → 开发主分支
feature/*   → 功能开发分支（从 develop 切出）
hotfix/*    → 紧急修复分支（从 main 切出）
release/*   → 发布分支（从 develop 切出）
```

### 环境划分

| 环境 | 后端端口 | 用途 |
|------|---------|------|
| dev | 8282 | 本地开发 |
| staging | - | 预发布测试 |
| prod | - | 生产环境 |

### 命名规范

```
后端（Go）:
- 文件名: snake_case
- 变量/函数: camelCase
- 结构体/类型: PascalCase
- 常量: PascalCase 或 ALL_CAPS

Flutter（Dart）:
- 文件名: snake_case
- 变量/函数: camelCase
- 类名: PascalCase
- 常量: lowerCamelCase 或 kPrefixCamelCase

Vben Admin（TypeScript/Vue）:
- 文件名: kebab-case（组件可 PascalCase）
- 变量/函数: camelCase
- 类/接口/类型: PascalCase
- 常量: UPPER_SNAKE_CASE
- 组件: PascalCase
```

